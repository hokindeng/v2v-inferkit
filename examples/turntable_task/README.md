# turntable domain

One folder per task. Each task folder holds prompt.txt plus first_video.mp4
(the v2v conditioning input); anything else is optional. `turntable_0000` is
the kit's smoke-test task — object permanence through a full turntable
rotation with a partial occluder.
